# Stage2 书籍元数据引用记录接入优化报告

## 基本信息

- 日期: 2026-04-25
- 类型: 工作流 Stage 2 衔接优化
- 锚点: `docs/project/MODULE_OPTIMIZATION_DESIGN_2026-04-21.md`
- 状态: 已完成

## 优化目标

前一步已将 `BookCitationOrganizer` 升级为统一 citation record 门面，但 Stage 2 仍未真正消费 `ResearchProject.book_metadata`。本步骤将书籍元数据接入 Stage 2，使模块优化进入工作流主链。

## 修改范围

- 更新 `tools/workflow/stages/stage2_organize.py`
- 更新 `tests/test_stage2_note_chain.py`
- 更新 `docs/project/MODULE_OPTIMIZATION_DESIGN_2026-04-21.md`

## 关键结果

1. 修复 `_get_book_organizer()` 的旧参数名，改为新版 `input_dir`。
2. `_process_book_metadata()` 从占位实现变为真实转换逻辑。
3. Stage 2 可将 `ResearchProject.book_metadata` 转换为统一 `book_citation_records`。
4. 书籍引用会扩展进入 `project.formatted_citations`，供后续 Stage 4/5/7 使用。
5. 不完整书籍元数据进入 `review_queue` 与 `quality_flags`。
6. Stage 2 metadata 现在记录 `book_metadata.total_books`、`records_needing_review`、`record_count`。

## 隐私合规

- 未读取或记录 `secrets/` 内容。
- 本步骤不触发远程 LLM。
- 测试仅使用内存构造的书籍元数据，无外部史料原文。
- 未生成临时脚本或持久中间文件。

## 验证结果

- `python -m py_compile tools\workflow\stages\stage2_organize.py tests\test_stage2_note_chain.py`
- `python -m unittest tests.test_stage2_note_chain`
- `python -m unittest tests.test_book_citation_organizer_facade tests.test_citation_chain tests.test_citation_normalizer_schema`
- `py -3.11 -m py_compile tools\workflow\stages\stage2_organize.py tests\test_stage2_note_chain.py`
- `py -3.11 -m unittest tests.test_stage2_note_chain tests.test_book_citation_organizer_facade`
- `py -3.11 -c "from tools.workflow.stages.stage2_organize import Stage2Organize; from modules.book_citation_organizer import BookCitationOrganizer; print('stage2_book_chain_import_ok')"`

## 后续动作

后续可继续把 Stage 2 的 citation records 与 Stage 4 引文网络、Stage 7 引文格式化输出完全打通，减少文献 citation 与书籍 citation 的并行结构。
